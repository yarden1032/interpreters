(define append 
  (lambda(lst1 lst2)
      @TODO
      (lambda(lst1 lst2)
    (if (empty? lst2) lst1 (append lst1 (cdr lst2)))
  )
)

(define reverse 
  (lambda (lst)
        @TODO
  )
)

(define duplicate-items
   (lambda(lst dup-count)
      @TODO
   )
)


(define payment
  (lambda (n coins-lst)
        @TODO
  )
)
                               
(define compose-n
  (lambda( f n)
        @TODO
  )
)